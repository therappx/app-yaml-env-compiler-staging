name: 'GAE Staging environment variable compiler'
description: 'Compiles and swaps environment variables in app-staging.yaml with the minimal effort'
branding:
  icon: 'cloud'
  color: 'blue'
runs:
  using: 'docker'
  image: 'Dockerfile'
